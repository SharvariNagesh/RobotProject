apply plugin: 'java'

repositories {
    mavenCentral()
    }

dependencies {
  testCompile group: 'junit', name: 'junit', version: '4.11'
}

test {
    testLogging {
            events "failed
	    exceptionFormat "short"

    }
}
